const namespace = 'br.com.unicopagapp';

export const SPLASH = `${namespace}.Splash`;
export const LOGIN = `${namespace}.Login`;
export const DASHBOARD = `${namespace}.Dashboard`;
export const PROFILE = `${namespace}.Profile`;
export const MAIN = `${namespace}.Main`;
export const RECOVER_PASSWORD = `${namespace}.RecoverPassword`;
export const CREATE_ACCOUNT = `${namespace}.CreateAccount`;
export const AUTH = `${namespace}.Auth`;
export const HOME = `${namespace}.Home`;
